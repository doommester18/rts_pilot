------
name: Build and push develop docker image to GCR
on:
  push:
    tags: // ne legyen pr-hez is kötve ?         
      - dev
      - test
     
jobs:
  build-platform-image:
    if: ${{ github.ref }} == refs/tags/dev
    name: Build saas-platform docker image
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: saas-platform
      PROJECT_ID: $PROJECT_ID
    steps:
    - name: Checkout saas-platform branch
      uses: actions/checkout@v2
  
  push-platform-image-to-gcr:

  run-test-collection:
  //át kell hívni a terraform repo-ba


---
name: Build and push release docker image to GCR
on:
  push:
    tags:        
      - pre-release
    branches: [ master ]

jobs:
  build-platform-image:
    if: ${{ github.ref }} == refs/tags/dev
    name: Build saas-platform docker image
    runs-on: ubuntu-latest
    env:
      IMAGE_NAME: saas-platform
      PROJECT_ID: $PROJECT_ID
    steps:
    - name: Checkout saas-platform branch
      uses: actions/checkout@v2
  
  push-platform-image-to-gcr:

  run-test-collection:
  //át kell hívni a terraform repo-ba



    - uses: google-github-actions/setup-gcloud@master
      with:
        service_account_key: ${{ secrets.SERVICE_ACCOUNT_KEY }}
        project_id: ${{ env.PROJECT_ID }}
        export_default_credentials: true

    - name: Build Docker Image
      run: docker build -t $IMAGE_NAME:latest .

    - name: Automatic Tagging of Releases
      id: increment-git-tag
      run: |
        bash ./scripts/git_update.sh -v major
    - name: Configure Docker Client
      run: |-
        gcloud auth configure-docker --quiet
        gcloud auth configure-docker us-west2-docker.pkg.dev --quiet
    - name: Push Docker Image to Container Registry (GCR)
      env:
        GIT_TAG: ${{ steps.increment-git-tag.outputs.git-tag }}
      run: |-
        docker tag $IMAGE_NAME:latest gcr.io/$PROJECT_ID/$IMAGE_NAME:latest
        docker tag $IMAGE_NAME:latest gcr.io/$PROJECT_ID/$IMAGE_NAME:$GIT_TAG
        docker push gcr.io/$PROJECT_ID/$IMAGE_NAME:latest
        docker push gcr.io/$PROJECT_ID/$IMAGE_NAME:$GIT_TAG
    - name: Push Docker Image to Artifact Registry
      env:
        GIT_TAG: ${{ steps.increment-git-tag.outputs.git-tag }}
      run: |-
        docker tag $IMAGE_NAME:latest us-west2-docker.pkg.dev/$PROJECT_ID/images/$IMAGE_NAME:latest
        docker tag $IMAGE_NAME:latest us-west2-docker.pkg.dev/$PROJECT_ID/images/$IMAGE_NAME:$GIT_TAG
        docker push us-west2-docker.pkg.dev/$PROJECT_ID/images/$IMAGE_NAME:latest
        docker push us-west2-docker.pkg.dev/$PROJECT_ID/images/$IMAGE_NAME:$GIT_TAG