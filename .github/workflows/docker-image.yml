name: Build and push develop docker image to GCR

on:
  push:
    tags:
    - dev**
    - test**
    - pre-release**

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Build the Docker image
      run: |
        docker build ./nodejs -t template
